# datus-db-core

Core database abstractions for Datus adapters. Provides `BaseSqlConnector`, `ConnectorRegistry`, SQL parsing utilities, and shared models used by all database adapter packages.

## Installation

```bash
pip install datus-db-core
```
