[project]
name = "datus-hologres"
version = "0.1.0"
description = "Alibaba Cloud Hologres database adapter for Datus"
readme = "README.md"
requires-python = ">=3.12"
license = {text = "Apache-2.0"}
authors = [
    {name = "DatusAI", email = "support@datus.ai"}
]
keywords = ["datus", "database", "hologres", "postgresql", "adapter"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
]

dependencies = [
    "datus-db-core>=0.1.5",
    "datus-postgresql>=0.1.8",
    "pydantic>=2.0.0",
]

[project.urls]
Homepage = "https://github.com/Datus-ai/datus-db-adapters"
Repository = "https://github.com/Datus-ai/datus-db-adapters"
Issues = "https://github.com/Datus-ai/datus-db-adapters/issues"

[project.entry-points."datus.adapters"]
hologres = "datus_hologres:register"

[tool.uv.sources]
datus-db-core = { workspace = true }
datus-postgresql = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["datus_hologres"]

[tool.pytest.ini_options]
markers = [
    "integration: marks tests that require a live Hologres instance",
    "acceptance: marks core adapter contract tests",
]
testpaths = ["tests"]
